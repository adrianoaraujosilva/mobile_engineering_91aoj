import 'package:flutter/material.dart';

const addIcon = Icon(Icons.add);
const loginPage = Text('Login Page');
const textPreenchaInformacoes = Text('Por favor, preencha as credenciais');
const btnLogar = Text('Entrar');
