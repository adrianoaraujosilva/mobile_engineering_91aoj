import 'package:flutter/material.dart';

const sizedBox = SizedBox(height: 10);
const loginPage = Text('Login Page');
const btnLogar = Text('Entrar');
const edgeInsets = EdgeInsets.all(10.0);
const outlineInputBorder = OutlineInputBorder();

const addIcon = Icon(Icons.add);
const iconText = Icon(Icons.text_fields);
const iconCalendar = Icon(Icons.calendar_today);
const iconNumber = Icon(Icons.numbers);
const iconCurrency = Icon(Icons.currency_exchange);
